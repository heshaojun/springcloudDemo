server:
  port: 9990
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/task_db?useSSL=false&characterEncoding=utf-8
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update

  cloud:
    task:
      initialize-enabled: true
